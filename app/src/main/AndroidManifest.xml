<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 14 Android Permissions for MAYRA Assistant -->
    <!-- 1. Default Assistant / Voice Interaction -->
    <uses-permission android:name="android.permission.BIND_VOICE_INTERACTION" />
    
    <!-- 2. Microphone & Audio -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- 3. Camera -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- 4. Phone Calls -->
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!-- 5. Location -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- 6. Contacts -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- 7. SMS -->
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />

    <!-- 8. Gallery & Files -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <!-- 9. Answer & Manage Calls -->
    <uses-permission android:name="android.permission.ANSWER_PHONE_CALLS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />

    <!-- 10. Notification Access -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE" />

    <!-- 11. Accessibility Service -->
    <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE" />

    <!-- 12. Battery Optimization Exemption -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- 13. Display Over Other Apps (Overlay) -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- 14. Foreground Service & Media Projection -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name="com.mayra.assistant.MayraApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="MAYRA"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mayra">

        <activity
            android:name="com.mayra.assistant.MainActivity"
            android:exported="true"
            android:label="MAYRA"
            android:theme="@style/Theme.Mayra">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MAYRA Microphone Persistent Listening Foreground Service -->
        <service
            android:name="com.mayra.assistant.services.MayraMicrophoneForegroundService"
            android:label="MAYRA Voice Listener"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- MAYRA Voice Interaction Service (Default Digital Assistant) -->
        <service
            android:name="com.mayra.assistant.services.MayraVoiceInteractionService"
            android:label="MAYRA"
            android:permission="android.permission.BIND_VOICE_INTERACTION"
            android:exported="true">
            <meta-data
                android:name="android.voice_interaction"
                android:resource="@xml/voice_interaction_service" />
            <intent-filter>
                <action android:name="android.service.voice.VoiceInteractionService" />
            </intent-filter>
        </service>

        <!-- MAYRA Accessibility Service (Task Automation) -->
        <service
            android:name="com.mayra.assistant.services.MayraAccessibilityService"
            android:label="MAYRA Automation Service"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- MAYRA Notification Listener -->
        <service
            android:name="com.mayra.assistant.services.MayraNotificationService"
            android:label="MAYRA Notification Guardian"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- MAYRA InCallService (Call Handling & Automation) -->
        <service
            android:name="com.mayra.assistant.services.MayraInCallService"
            android:label="MAYRA Call Manager"
            android:permission="android.permission.BIND_INCALL_SERVICE"
            android:exported="true">
            <meta-data
                android:name="android.telecom.IN_CALL_SERVICE_UI"
                android:value="true" />
            <intent-filter>
                <action android:name="android.telecom.InCallService" />
            </intent-filter>
        </service>

        <!-- MAYRA Background Gesture Floating Overlay & Camera Foreground Service -->
        <service
            android:name="com.mayra.assistant.services.MayraBackgroundGestureOverlayService"
            android:label="MAYRA Background Gesture Controller"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="camera" />

        <!-- MAYRA System-Wide Gesture Accessibility Service (Synthetic Touch Dispatcher) -->
        <service
            android:name="com.mayra.assistant.services.MayraGestureAccessibilityService"
            android:label="MAYRA System Gesture Touch Controller"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/gesture_accessibility_service_config" />
        </service>

    </application>

</manifest>
